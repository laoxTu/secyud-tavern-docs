# Builder（上下文构建器）

控制如何将对话历史和激活的世界书拼接为发送给 LLM 的消息数组。

## 默认构建器

系统内置默认构建器，按以下顺序拼接：

1. 注入 System 角色的世界书（layer < 100 置前，≥ 100 置后）
2. 合并同角色连续消息（减少 token 消耗）
3. 追加当前用户输入

Builder 支持通过插件扩展，实现自定义的上下文拼接逻辑。
